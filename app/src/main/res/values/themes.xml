<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScriptDistribution" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ScriptDistribution" parent="Base.Theme.ScriptDistribution" />

    <!-- Bottom sheet theme for GenerateSidesDialog -->
    <style name="Theme.ScriptDistribution.BottomSheet" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.ScriptDistribution.BottomSheet</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
    </style>

    <style name="Widget.ScriptDistribution.BottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="backgroundTint">@color/bg_primary</item>
        <item name="shapeAppearance">@style/ShapeAppearance.ScriptDistribution.BottomSheet</item>
    </style>

    <style name="ShapeAppearance.ScriptDistribution.BottomSheet" parent="ShapeAppearance.Material3.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">20dp</item>
        <item name="cornerSizeTopRight">20dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>
</resources>